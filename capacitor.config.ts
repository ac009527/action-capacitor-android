import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'action-capacitor-android',
  webDir: 'dist'
};

export default config;
